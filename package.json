{
  "name": "@ignite-ui/global",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --verbose"
  },
  "devDependencies": {
    "turbo": "^2.0.10"
  },
  "packageManager": "npm@10.8.2"
}